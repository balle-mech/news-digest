{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git fetch origin && (git rev-parse --verify refs/heads/main > /dev/null 2>&1 && git checkout main && git merge --ff-only origin/main || git checkout -b main --track origin/main) 2>/dev/null || true",
            "statusMessage": "リモートの main ブランチを同期中..."
          }
        ]
      }
    ]
  }
}
